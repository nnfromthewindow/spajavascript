
const NAME = "fakestoreapi",
DOMAIN = `https://${NAME}.com`,
PRODUCTS = `${DOMAIN}/products`,
CATEGORIES = `${DOMAIN}/categories`,
USERS = `${DOMAIN}/users`

export default {
    NAME,
    DOMAIN,
    PRODUCTS,
    CATEGORIES,
    USERS
}